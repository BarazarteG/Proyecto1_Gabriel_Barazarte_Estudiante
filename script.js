for (let iterador = 1; iterador <= 20; iterador++){

    if (iterador%2 == 0){
        console.log(`${iterador} es par`);
        }else{
            console.log(`${iterador} es impar`);        
        }
}